using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class cur_angles_text : MonoBehaviour
{
    public Text angles;
    // Start is called before the first frame update
    void Start()
    {
        angles.text = "holq";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
